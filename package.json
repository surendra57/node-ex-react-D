{
  "name": "react",
  "version": "1.0.0", 
  "description": "",
  "main": "./server/indexx.js",
  "engines": {
    "node": "v16.14.0"
  },
  "scripts": {
    "start": "node ./server/indexx.js",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false --prefix clients &&  npm run build --prefix clients",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^2.0.15"
  }
}
